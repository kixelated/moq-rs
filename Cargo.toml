[workspace]
members = [
	"moq-transport",
	"moq-relay",
	"moq-pub",
	"moq-clock",
	"moq-dir",
	"moq-native",
	"moq-api",
]
resolver = "2"

# Use debug symbols in production until things are more stable
[profile.release]
debug = true
